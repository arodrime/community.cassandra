# Reads a running cluster into an inventory for the roles, changing nothing
# on the nodes:
#   ansible-playbook -i node1,node2 community.cassandra.import_cluster
# Any reachable nodes will do: the others are found with nodetool status.
# Writes hosts.yml, group_vars/, host_vars/ and report.txt to
# import_cluster_dir (default ./<cluster name>), which must not exist yet
# unless import_cluster_force is true.
# Passwords found in the config go to secrets.yml files next to the other
# variables: encrypted with ansible-vault when import_cluster_vault_password_file
# (and optionally import_cluster_vault_id) is given, else written 0600 for you
# to encrypt. JMX access: the cassandra_service variables (cassandra_jmx_port,
# cassandra_jmx_username, cassandra_jmx_password_file...).
- name: Find the cluster from the given nodes
  hosts: all
  gather_facts: true
  gather_subset: [network]
  tasks:
    - name: Load the cassandra_service defaults (JMX access)
      ansible.builtin.include_role:
        name: community.cassandra.cassandra_service
        tasks_from: defaults.yml
        public: true

    - name: Remember the given nodes (add_host extends the play)
      ansible.builtin.set_fact:
        import_cluster_given: "{{ ansible_play_hosts }}"
      run_once: true

    - name: Read the ring
      community.cassandra.cassandra_status:
        down: 10000  # read it whatever the state of the nodes
      module_defaults:
        community.cassandra.cassandra_status: "{{ _cassandra_service_jmx }}"
      register: import_cluster_ring
      failed_when: false

    - name: Add the nodes the inventory does not have yet
      ansible.builtin.add_host:
        name: "{{ item.address }}"
        ansible_host: "{{ item.address }}"
        groups: [import_cluster_found]
      loop: "{{ _ring }}"
      run_once: true
      when: item.address not in _known
      vars:
        _status: >-
          {{ import_cluster_given | map('extract', hostvars) | map(attribute='import_cluster_ring.cluster_status', default={})
             | select | first | default({}) }}
        _ring: "{{ _status | community.cassandra.cassandra_ring_nodes }}"
        _known: "{{ import_cluster_given | map('extract', hostvars, ['ansible_facts', 'all_ipv4_addresses']) | flatten }}"

    - name: Stop if no node answered
      ansible.builtin.assert:
        that: import_cluster_given | map('extract', hostvars) | map(attribute='import_cluster_ring.cluster_status', default={}) | select | list | length > 0
        fail_msg: nodetool status failed on every given node, is Cassandra running?
      run_once: true

- name: Read every node
  hosts: all
  gather_facts: false
  ignore_unreachable: true
  become: true
  vars:
    _files:
      - cassandra.yaml
      - cassandra-env.sh
      - jvm-server.options
      - jvm8-server.options
      - jvm11-server.options
      - jvm17-server.options
      - cassandra-rackdc.properties
      - logback.xml
  tasks:
    - name: Load the cassandra_service defaults (JMX access)
      ansible.builtin.include_role:
        name: community.cassandra.cassandra_service
        tasks_from: defaults.yml
        public: true

    - name: Gather facts
      ansible.builtin.setup:
        gather_subset: [network, distribution]

    - name: Read the installed packages
      ansible.builtin.package_facts:

    # No module reads a running JVM: its command line and its java binary
    - name: Read the running Cassandra (conf dir, Cassandra and Java versions)
      ansible.builtin.shell: |
        pid=$(pgrep -f CassandraDaemon | head -1)
        [ -n "$pid" ] || exit 3
        echo "config=$(tr '\0' '\n' < /proc/$pid/cmdline | sed -n 's#^-Dcassandra.config=\(file://\)\?##p' | head -1)"
        echo "version=$({{ _cassandra_service_nodetool }} version | sed -n 's/^ReleaseVersion: //p')"
        echo "java=$($(readlink -f /proc/$pid/exe) -XshowSettings:properties -version 2>&1 | sed -n 's/.*java.specification.version = //p')"
      register: import_cluster_running
      changed_when: false
      failed_when: false
      check_mode: false

    - name: Work out the series and the conf dir
      ansible.builtin.set_fact:
        import_cluster_version: "{{ _kv.version }}"
        import_cluster_series: "{{ _kv.version.split('.')[:2] | join('') ~ 'x' }}"
        import_cluster_java: "{{ _kv.java.split('.')[1] if _kv.java.startswith('1.') else _kv.java }}"
        import_cluster_package: "{{ (ansible_facts.packages['cassandra'] | default([{}]) | first).version | default('') }}"
        import_cluster_conf_dir: >-
          {{ _kv.config | dirname if _kv.config is match('/')
             else ('/etc/cassandra/conf' if ansible_facts['os_family'] == 'RedHat' else '/etc/cassandra') }}
      vars:
        _kv: "{{ dict(import_cluster_running.stdout_lines | map('split', '=', 1)) }}"
      when: import_cluster_running.rc | default(1) == 0

    - name: Read the config files
      ansible.builtin.slurp:
        path: "{{ import_cluster_conf_dir }}/{{ item }}"
      loop: "{{ _files }}"
      register: import_cluster_slurp
      failed_when: false
      no_log: true  # keystore passwords
      when: import_cluster_series is defined and import_cluster_series in ['40x', '41x', '50x']

    - name: Turn them into cassandra_config variables
      no_log: true
      ansible.builtin.set_fact:
        import_cluster_config: "{{ _live | community.cassandra.cassandra_config_import(import_cluster_series, ansible_facts) }}"
      vars:
        _live: >-
          {{ dict(import_cluster_slurp.results | selectattr('content', 'defined')
                  | map(attribute='item')
                  | zip(import_cluster_slurp.results | selectattr('content', 'defined')
                        | map(attribute='content') | map('b64decode'))) }}
      when: import_cluster_series is defined and import_cluster_slurp.results is defined

- name: Write the inventory
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    _given: "{{ hostvars[groups['all'][0]]['import_cluster_given'] }}"
    _ring: >-
      {{ _given | map('extract', hostvars) | map(attribute='import_cluster_ring.cluster_status', default={})
         | select | first | default({}) | community.cassandra.cassandra_ring_nodes }}
    _java_default: {40x: "11", 41x: "11", 50x: "17"}
    _vault: "{{ import_cluster_vault_password_file | default('') != '' }}"
    _files: "{{ _layout | community.cassandra.cassandra_inventory_files }}"
    _secrets: "{{ _files | selectattr('secret') | map(attribute='path') | list }}"
    _report: |-
      {{ _layout.report }}
      {% if _secrets %}
      PASSWORDS found in the config, in {{ _secrets | join(', ') }}:
      {% if _vault %}
        encrypted with ansible-vault (password file {{ import_cluster_vault_password_file }}).
      {% else %}
        written in clear, mode 0600. Encrypt them before anything else:
          cd {{ _dir }} && ansible-vault encrypt {{ _secrets | join(' ') }}
      {% endif %}
      {% endif %}

      Next steps:
        1. Review the report above and the variables (hand edits, drift).
        2. Check the cluster: ansible-playbook -i {{ _dir }}/hosts.yml community.cassandra.preflight -e cassandra_hosts={{ _layout.cluster_group }}
        3. See what the roles would change, changing nothing:
           ansible-playbook -i {{ _dir }}/hosts.yml <your site playbook> --check --diff -e cassandra_hosts={{ _layout.cluster_group }}
  tasks:
    - name: Match the ring with the hosts
      no_log: true
      ansible.builtin.set_fact:
        _nodes: "{{ _nodes | default([]) + [_node] }}"
      loop: "{{ _ring }}"
      loop_control:
        label: "{{ item.address }}"
      vars:
        _host: >-
          {{ _given | map('extract', hostvars) | selectattr('ansible_facts.all_ipv4_addresses', 'defined')
             | selectattr('ansible_facts.all_ipv4_addresses', 'contains', item.address)
             | map(attribute='inventory_hostname') | first | default(item.address) }}
        _hv: "{{ hostvars[_host] | default({}) }}"
        _read: "{{ _hv.import_cluster_config is defined }}"
        _name: >-
          {{ _host if _host != item.address
             else (_hv.ansible_facts.hostname | default(item.address)) }}
        _java: >-
          {{ {'cassandra_java_version': _hv.import_cluster_java}
             if _read and _hv.import_cluster_java != _java_default[_hv.import_cluster_series] | default('') else {} }}
        # the installed version, so nodes added later get the same one
        _pkg: >-
          {{ {'cassandra_package_version': _hv.import_cluster_package | regex_replace('^([0-9]+\.[0-9]+\.[0-9]+).*$', '\1')}
             if _read and _hv.import_cluster_package | default('') else {} }}
        _conf: >-
          {{ {'cassandra_conf_dir': _hv.import_cluster_conf_dir}
             if _read and _hv.import_cluster_conf_dir not in ['/etc/cassandra', '/etc/cassandra/conf'] else {} }}
        _node:
          name: "{{ _name }}"
          ansible_host: "{{ item.address if _host == item.address else (_hv.ansible_host | default('')) }}"
          dc: "{{ item.dc }}"
          rack: "{{ item.rack }}"
          read: "{{ _read }}"
          reason: >-
            {{ 'unreachable' if _hv.import_cluster_running.rc is not defined
               else ('Cassandra not running' if _hv.import_cluster_running.rc != 0
               else 'unsupported version ' ~ _hv.import_cluster_version) }}
          vars: >-
            {{ (_hv.import_cluster_config.vars | combine({'cassandra_version': _hv.import_cluster_series}, _java, _pkg, _conf))
               if _read else {} }}
          hand_edits: "{{ _hv.import_cluster_config.hand_edits | default([]) }}"
          normalized: "{{ _hv.import_cluster_config.normalized | default([]) }}"
          notes:
            - "{{ _versions ~ ', ' ~ _install ~ ', config in ' ~ _hv.import_cluster_conf_dir | default('?') }}"
        _versions: "{{ 'Cassandra ' ~ _hv.import_cluster_version | default('?') ~ ', Java ' ~ _hv.import_cluster_java | default('?') }}"
        _install: >-
          {{ ('package ' ~ _hv.import_cluster_package) if _hv.import_cluster_package | default('')
             else 'NOT a package install: cassandra_install would install the package next to it' }}

    - name: Lay out groups and variables
      no_log: true
      ansible.builtin.set_fact:
        _layout: "{{ _nodes | community.cassandra.cassandra_inventory_layout(_cluster_name) }}"
      vars:
        _cluster_name: >-
          {{ _nodes | selectattr('read') | map(attribute='vars.cassandra_cluster_name') | first | default('cassandra') }}

    - name: Pick the output dir
      ansible.builtin.set_fact:
        _dir: "{{ import_cluster_dir | default(lookup('env', 'PWD') ~ '/' ~ _layout.cluster_group) }}"

    - name: Check the output dir
      ansible.builtin.stat:
        path: "{{ _dir }}"
      register: import_cluster_dir_stat

    - name: Stop rather than overwrite an inventory
      ansible.builtin.assert:
        that: not import_cluster_dir_stat.stat.exists or import_cluster_force | default(false) | bool
        fail_msg: "{{ _dir }} exists: pick another import_cluster_dir, or set import_cluster_force=true"

    - name: Create the dirs
      ansible.builtin.file:
        path: "{{ _dir }}/{{ item }}"
        state: directory
        mode: "0755"
      loop: "{{ [''] + _files | map(attribute='path') | map('dirname') | unique | list }}"

    - name: Write hosts.yml
      ansible.builtin.copy:
        content: "{{ _layout.hosts | to_nice_yaml(indent=2, sort_keys=false) }}"
        dest: "{{ _dir }}/hosts.yml"
        mode: "0644"

    # secrets.yml: encrypted as a whole (Ansible reads vaulted vars files), or 0600 in clear
    - name: Write group_vars and host_vars
      ansible.builtin.copy:
        content: >-
          {{ item.content | ansible.builtin.vault(lookup('ansible.builtin.file', import_cluster_vault_password_file),
                                                    vault_id=import_cluster_vault_id | default('default'))
             if item.secret and _vault else item.content }}
        dest: "{{ _dir }}/{{ item.path }}"
        mode: "{{ '0600' if item.secret else '0644' }}"
        decrypt: false  # the content is already encrypted, write it as is
      loop: "{{ _files }}"
      loop_control:
        label: "{{ item.path }}"
      no_log: "{{ item.secret }}"

    - name: Write report.txt
      ansible.builtin.copy:
        content: "{{ _report }}\n"
        dest: "{{ _dir }}/report.txt"
        mode: "0644"

    - name: Show the report
      ansible.builtin.debug:
        msg: "{{ (_report ~ '\nWritten to ' ~ _dir).split('\n') }}"
